# Define Stages
stages:
  - Build Dependencies 1
  - Build Dependencies 2
  - Build Dependencies 3
  - Build
  - Artifact Upload

#before_script:
#  - sample

# Set variables inherited by all jobs
variables:
  GIT_DEPTH: 5 # Only grab the last 5 commits when cloning
  NEXUS_BASEURL: 'https://nexus.int.windscribe.com'
  NEXUS_PATH_ROOT: '/repository/$CI_PROJECT_NAME/$CI_PROJECT_NAME/'
  NEXUS_PATH_LATEST: '/latest'
  NEXUS_PATH_BRANCHES: '/branches'
  NEXUS_PATH_TAGS: '/tags'
  NEXUS_PATH_DEPS: '/repository/$CI_PROJECT_NAME/$CI_PROJECT_NAME/dependencies'
  NEXUS_PATH_DEPS_LATEST: '/latest'
  NEXUS_PATH_DEPS_BRANCHES: '/branches'
  NEXUS_PATH_DEPS_TAGS: '/tags'
  TARGET: '*.exe'
  #NEXUS_USERNAME: "sample"         # Set in Gitlab variables
  #NEXUS_PASSWORD: "VhVvMiM3jD04c"  # Set in Gitlab variables
  CODE_SIGNING_PFX_PATH: 'installer\windows\signing\code_signing.pfx'
  RUNNING_CI: 1
  BUILDROOT: 'build-libs'
  TMPROOT: 'temp'
  ARTIFACT_INSTALLER: 'installer.exe'
  BUILD_WIN: 'y'
  BUILD_MAC: 'y'

include:
  - local: 'vars.yml'

.template_win10_build: &template_win10_build
  tags: [win10]
  before_script:
    # run this blindly until baked into image
    - python tools/bin/get-pip.py
    - python -m pip install -r tools/requirements.txt


.template_mac_build: &template_mac_build
  tags: [macos11]
  before_script:
    # run this blindly until baked into image
    - python tools/bin/get-pip.py
    - python -m pip install -r tools/requirements.txt


# Build Dependency and Upload Jom
build:win:jom:
  <<: *template_win10_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_JOM
  script:
    - tools/deps/install_jom -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/jom.yml

# No jom for mac


# Build Dependency and Upload Cares
build:win:cares:
  <<: *template_win10_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_CARES
  script:
    - tools/deps/install_cares -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/cares.yml


build:mac:cares:
  <<: *template_mac_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_CARES
  script:
    - tools/deps/install_cares -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/cares.yml


# Build Dependency and Upload Lzo
build:win:lzo:
  <<: *template_win10_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_LZO
  script:
    - tools/deps/install_lzo -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/lzo.yml


build:mac:lzo:
  <<: *template_mac_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_LZO
  script:
    - tools/deps/install_lzo -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/lzo.yml


# Build Dependency and Upload Boost
build:win:boost:
  <<: *template_win10_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_BOOST
  script:
    - tools/deps/install_boost -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/boost.yml


build:mac:boost:
  <<: *template_mac_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_BOOST
  script:
    - tools/deps/install_boost -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/boost.yml

# Build Dependency and Upload Wireguard
build:win:wireguard:
  <<: *template_win10_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_WIREGUARD
  script:
    - tools/deps/install_wireguard -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/wireguard.yml


build:mac:wireguard:
  <<: *template_mac_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_WIREGUARD
  script:
    - tools/deps/install_wireguard -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/wireguard.yml

# Build Dependency and Upload Gtest
build:win:gtest:
  <<: *template_win10_build
  stage: Build Dependencies 1
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_GTEST
  script:
    - tools/deps/install_gtest -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/gtest.yml


#build:mac:gtest:
#  <<: *template_mac_build
#  stage: Build Dependencies 1
#  variables:
#    GIT_STRATEGY: clone
#    ARTIFACT_PATH: $ARTIFACT_GTEST
#  script:
#    - tools/deps/install_gtest -zip
#    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
#  rules:
#    - if: '$BUILD_MAC == "y"'
#      changes:
#        - tools/vars/gtest.yml


# Build Dependency and Upload Openssl
build:win:openssl:
  <<: *template_win10_build
  stage: Build Dependencies 2
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_OPENSSL
  # needs:
    # - {job: 'build:win:jom', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_JOM "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_JOM}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - tools/deps/install_openssl -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/openssl.yml
        - tools/vars/jom.yml


build:mac:openssl:
  <<: *template_mac_build
  stage: Build Dependencies 2
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_OPENSSL
  script:
    - tools/deps/install_openssl -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/openssl.yml

# Build Dependency and Upload Zlib
build:win:zlib:
  <<: *template_win10_build
  stage: Build Dependencies 2
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_ZLIB
  # needs:
    # - {job: 'build:win:jom', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_JOM "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_JOM}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - tools/deps/install_zlib -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/zlib.yml
        - tools/vars/jom.yml

# No zlib for mac


# Build and Upload Curl
build:win:curl:
  <<: *template_win10_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_CURL
  # needs:
    # - {job: 'build:win:openssl', optional: true}
    # - {job: 'build:win:zlib', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_OPENSSL}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_ZLIB "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_ZLIB}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - tools/deps/install_curl -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/curl.yml
        - tools/vars/zlib.yml
        - tools/vars/openssl.yml


build:mac:curl:
  <<: *template_mac_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_CURL
  # needs:
    # - {job: 'build:mac:openssl', optional: true}
  script:
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_OPENSSL}"
    - if [ -d ./build-libs/ ]; then for z in ./build-libs/*.zip; do unzip -qod ${z%%.zip} $z; done; fi
    - tools/deps/install_curl -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/curl.yml
        - tools/vars/openssl.yml


# Build and Upload Openvpn
build:win:openvpn:
  <<: *template_win10_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_OPENVPN
  # needs:
    # - {job: 'build:win:openssl', optional: true}
    # - {job: 'build:win:lzo', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_OPENSSL}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_LZO "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_LZO}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - tools/deps/install_openvpn -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/openvpn.yml
        - tools/vars/openssl.yml
        - tools/vars/jom.yml
        - tools/vars/lzo.yml


build:mac:openvpn:
  <<: *template_mac_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_OPENVPN
  # needs:
    # - {job: 'build:mac:openssl', optional: true}
    # - {job: 'build:mac:lzo', optional: true}
  script:
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_OPENSSL}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_LZO "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_LZO}"
    - if [ -d ./build-libs/ ]; then for z in ./build-libs/*.zip; do unzip -qod ${z%%.zip} $z; done; fi
    - tools/deps/install_openvpn -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/openvpn.yml
        - tools/vars/openssl.yml
        - tools/vars/lzo.yml


# Build and Upload Protobuf
build:win:protobuf:
  <<: *template_win10_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_PROTOBUF
  # needs:
    # - {job: 'build:win:zlib', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_ZLIB "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_ZLIB}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - tools/deps/install_protobuf -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/protobuf.yml
        - tools/vars/zlib.yml


build:mac:protobuf:
  <<: *template_mac_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_PROTOBUF
  script:
    - tools/deps/install_protobuf -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/protobuf.yml


# Build and Upload Qt
build:win:qt:
  <<: *template_win10_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_QT
  # needs:
    # - {job: 'build:win:jom', optional: true}
    # - {job: 'build:win:openssl', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_OPENSSL}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_JOM "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_JOM}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - tools/deps/install_qt -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/qt.yml
        - tools/vars/openssl.yml
        - tools/vars/jom.yml


build:mac:qt:
  <<: *template_mac_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_QT
  # needs:
    # - {job: 'build:mac:openssl', optional: true}
  script:
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_OPENSSL}"
    - if [ -d ./build-libs/ ]; then for z in ./build-libs/*.zip; do unzip -qod ${z%%.zip} $z; done; fi
    - tools/deps/install_qt
    - (cd ./build-libs/qt; zip --symlinks -r ../qt.zip .)
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/qt.yml
        - tools/vars/openssl.yml


# Build and Upload Stunnel
build:win:stunnel:
  <<: *template_win10_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_STUNNEL
  # needs:
    # - {job: 'build:win:jom', optional: true}
    # - {job: 'build:win:openssl', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_OPENSSL}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_JOM "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_JOM}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - tools/deps/install_stunnel -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_WIN == "y"'
      changes:
        - tools/vars/stunnel.yml
        - tools/vars/openssl.yml
        - tools/vars/jom.yml


build:mac:stunnel:
  <<: *template_mac_build
  stage: Build Dependencies 3
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_STUNNEL
  # needs:
    # - {job: 'build:mac:openssl', optional: true}
  script:
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_OPENSSL}"
    - if [ -d ./build-libs/ ]; then for z in ./build-libs/*.zip; do unzip -qod ${z%%.zip} $z; done; fi
    - tools/deps/install_stunnel -zip
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file $BUILDROOT/$ARTIFACT_PATH "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PATH}"
  rules:
    - if: '$BUILD_MAC == "y"'
      changes:
        - tools/vars/stunnel.yml
        - tools/vars/openssl.yml

# Build and Upload Installer
build:win:installer:
  <<: *template_win10_build
  stage: Build
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_INSTALLER
  # needs:
    # - {job: 'build:win:jom', optional: true}
    # - {job: 'build:win:cares', optional: true}
    # - {job: 'build:win:lzo', optional: true}
    # - {job: 'build:win:boost', optional: true}
    # - {job: 'build:win:wireguard', optional: true}
    # - {job: 'build:win:gtest', optional: true}
    # - {job: 'build:win:openssl', optional: true}
    # - {job: 'build:win:zlib', optional: true}
    # - {job: 'build:win:curl', optional: true}
    # - {job: 'build:win:openvpn', optional: true}
    # - {job: 'build:win:protobuf', optional: true}
    # - {job: 'build:win:qt', optional: true}
    # - {job: 'build:win:stunnel', optional: true}
  script:
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_JOM "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_JOM}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_CARES "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_CARES}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_LZO "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_LZO}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_BOOST "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_BOOST}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_WIREGUARD "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_WIREGUARD}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_GTEST "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_GTEST}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_OPENSSL}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_ZLIB "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_ZLIB}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_CURL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_CURL}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENVPN "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_OPENVPN}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_PROTOBUF "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_PROTOBUF}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_QT "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_QT}"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_STUNNEL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/${ARTIFACT_STUNNEL}"
    - IF(Test-Path .\build-libs\) {Get-ChildItem .\build-libs\*.zip | Foreach {.\tools\bin\7z.exe x $_.FullName $("-obuild-libs\"+$([io.path]::GetFileNameWithoutExtension($_.name)))}}
    - powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command .\.base64_decode.ps1
    - Set-Variable -name VERSION -value $(python installer/windows/extract_version.py common/version/windscribe_version.h)
    - tools/build_all -zip
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file build-exe/Windscribe_$VERSION.exe "${NEXUS_BASEURL}${NEXUS_PATH_ROOT}${NEXUS_PATH_BRANCHES}/${CI_COMMIT_BRANCH}/${DATE}/${CI_COMMIT_SHORT_SHA}/win/Windscribe_$VERSION.exe"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file build-exe/WindscribeSymbols_$VERSION.zip "${NEXUS_BASEURL}${NEXUS_PATH_ROOT}${NEXUS_PATH_BRANCHES}/${CI_COMMIT_BRANCH}/${DATE}/${CI_COMMIT_SHORT_SHA}/win/WindscribeSymbols_$VERSION.zip"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file build-exe/Windscribe_$VERSION.exe "${NEXUS_BASEURL}${NEXUS_PATH_ROOT}${NEXUS_PATH_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/Windscribe_$VERSION.exe"
    - tools/bin/curl.exe --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file build-exe/WindscribeSymbols_$VERSION.zip "${NEXUS_BASEURL}${NEXUS_PATH_ROOT}${NEXUS_PATH_BRANCHES}/${CI_COMMIT_BRANCH}/latest/win/WindscribeSymbols_$VERSION.zip"
  rules:
    - if: '$BUILD_WIN == "y"'
    # changes:
      # - '.gitlab-ci.yml'
      # - tools/vars/jom.yml
      # - tools/vars/cares.yml
      # - tools/vars/lzo.yml
      # - tools/vars/boost.yml
      # - tools/vars/wireguard.yml
      # - tools/vars/gtest.yml
      # - tools/vars/openssl.yml
      # - tools/vars/zlib.yml
      # - tools/vars/curl.yml
      # - tools/vars/openvpn.yml
      # - tools/vars/protobuf.yml
      # - tools/vars/qt.yml
      # - tools/vars/stunnel.yml


build:mac:installer:
  <<: *template_mac_build
  stage: Build
  variables:
    GIT_STRATEGY: clone
    ARTIFACT_PATH: $ARTIFACT_INSTALLER
  # needs:
    # - {job: 'build:mac:cares', optional: true}
    # - {job: 'build:mac:lzo', optional: true}
    # - {job: 'build:mac:boost', optional: true}
    # - {job: 'build:mac:wireguard', optional: true}
    # - {job: 'build:mac:gtest', optional: true}
    # - {job: 'build:mac:openssl', optional: true}
    # - {job: 'build:mac:curl', optional: true}
    # - {job: 'build:mac:openvpn', optional: true}
    # - {job: 'build:mac:protobuf', optional: true}
    # - {job: 'build:mac:qt', optional: true}
    # - {job: 'build:mac:stunnel', optional: true}
  script:
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_CARES "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_CARES}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_LZO "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_LZO}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_BOOST "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_BOOST}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_WIREGUARD "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_WIREGUARD}"
    # - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_GTEST "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_GTEST}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENSSL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_OPENSSL}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_CURL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_CURL}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_OPENVPN "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_OPENVPN}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_PROTOBUF "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_PROTOBUF}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_QT "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_QT}"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --create-dirs -o $BUILDROOT/$ARTIFACT_STUNNEL "${NEXUS_BASEURL}${NEXUS_PATH_DEPS}${NEXUS_PATH_DEPS_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/${ARTIFACT_STUNNEL}"
    - if [ -d ./build-libs/ ]; then for z in ./build-libs/*.zip; do unzip -qod ${z%%.zip} $z; done; fi
    # Clean out DropDMG Windscribe2 settings and symlink to this repo
    - rm -rf ~/Library/Application\ Support/DropDMG/Configurations/Windscribe2.plist
    - rm -rf ~/Library/Application\ Support/DropDMG/Layouts/Windscribe2
    - ln -s $(pwd)/common/prepare_build_environment/mac/DropDMG/Configurations/Windscribe2.plist ~/Library/Application\ Support/DropDMG/Configurations/Windscribe2.plist
    - ln -s $(pwd)/common/prepare_build_environment/mac/DropDMG/Layouts/Windscribe2 ~/Library/Application\ Support/DropDMG/Layouts/Windscribe2
    # - echo -n "$CODE_SIGNING_PFX_BASE64" | base64 --decode > $CODE_SIGNING_PFX_PATH
    - VERSION=$(python installer/windows/extract_version.py common/version/windscribe_version.h)
    - tools/build_all -zip --ci-mode --notarize
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file build-exe/Windscribe_$VERSION.dmg "${NEXUS_BASEURL}${NEXUS_PATH_ROOT}${NEXUS_PATH_BRANCHES}/${CI_COMMIT_BRANCH}/${DATE}/${CI_COMMIT_SHORT_SHA}/macos/Windscribe_$VERSION.dmg"
    - curl --silent --show-error --fail -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" --cacert tools/cacert.pem --upload-file build-exe/Windscribe_$VERSION.dmg "${NEXUS_BASEURL}${NEXUS_PATH_ROOT}${NEXUS_PATH_BRANCHES}/${CI_COMMIT_BRANCH}/latest/macos/Windscribe_$VERSION.dmg"
    - rm -rf ~/Library/Application\ Support/DropDMG/Configurations/Windscribe2.plist
    - rm -rf ~/Library/Application\ Support/DropDMG/Layouts/Windscribe2
  rules:
    - if: '$BUILD_MAC == "y"'
    # changes:
      # - '.gitlab-ci.yml'
      # - tools/vars/jom.yml
      # - tools/vars/cares.yml
      # - tools/vars/lzo.yml
      # - tools/vars/boost.yml
      # - tools/vars/wireguard.yml
      # - tools/vars/gtest.yml
      # - tools/vars/openssl.yml
      # - tools/vars/zlib.yml
      # - tools/vars/curl.yml
      # - tools/vars/openvpn.yml
      # - tools/vars/protobuf.yml
      # - tools/vars/qt.yml
      # - tools/vars/stunnel.yml

