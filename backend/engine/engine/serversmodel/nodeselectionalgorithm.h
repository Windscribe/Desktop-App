#ifndef NODESELECTIONALGORITHM_H
#define NODESELECTIONALGORITHM_H

#include <QVector>
//#include "engine/types/apiinfo.h"

class NodeSelectionAlgorithm
{
public:

    /*struct  NodeInfo
    {
        int weight;
        int userRating;
        int pingTime;
        bool isNodeSkipped;
    };

    static int selectRandomNodeBasedOnWeight(const QVector<ServerNode> &nodes, NodesSpeedRatings *nodesSpeedRating, QVector<double> &p);

    static int selectRandomNodeBasedOnLatency(QVector<NodeInfo> &nodes, QVector<double> &p);

    static void autoTest();

private:
    static int getRandomEvent(QVector<double> &p);

    static const double WEIGHT_NODE;
    static const double WEIGHT_RATING;
    static const double WEIGHT_LATENCY;*/
};

#endif // NODESELECTIONALGORITHM_H
