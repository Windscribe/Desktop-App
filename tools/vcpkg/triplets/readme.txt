Some custom vcpkg triplets that are not available out of the box.
In particular, it's a triplet for building a universal Mac architecture.
Also for all platforms we build only release builds(VCPKG_BUILD_TYPE=release).